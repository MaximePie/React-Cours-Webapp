/**
 * Export all config values
 */
export default function config() {
  return {
    MYSTERY_NOTE_RATIO: 5, // Percentage of apparition
    MYSTERY_NOTE_LENGTH: 5000,
    MYSTERY_NOTE_MULTIPLIER: 60, // Based on notes per seconds income
  }
}
